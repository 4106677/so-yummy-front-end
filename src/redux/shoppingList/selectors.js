export const getShoppingList = state => state.shoppingList.shoppingList;

export const selectIsLoading = state => state.shoppingList.loading;

export const selectError = state => state.shoppingList.error;
export const getIsLoading = state => state.shoppingList.loading;

